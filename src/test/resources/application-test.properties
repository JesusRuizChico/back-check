# Usar siempre una base PostgreSQL exclusiva para pruebas, nunca la de desarrollo.
spring.datasource.url=${TEST_DB_URL}
spring.datasource.username=${TEST_DB_USER}
spring.datasource.password=${TEST_DB_PASSWORD:}
